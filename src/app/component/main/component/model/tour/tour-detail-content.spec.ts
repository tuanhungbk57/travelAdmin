import { TourDetailContent } from './tour-detail-content';

describe('TourDetailContent', () => {
  it('should create an instance', () => {
    expect(new TourDetailContent()).toBeTruthy();
  });
});
